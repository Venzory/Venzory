# ============================================
# REQUIRED ENVIRONMENT VARIABLES (All Environments)
# ============================================

# Node Environment
# Must be 'development', 'production', or 'test'
NODE_ENV=development

# ============================================
# DATABASE CONFIGURATION
# ============================================

# Option A: LOCAL DEVELOPMENT (Docker) - Recommended for Dev/Test
# Run 'npm run db:setup:local' to create these databases.
# ------------------------------------------------------------------
# For development:
DATABASE_URL="postgresql://venzory:venzory@localhost:5432/venzory_dev"
DIRECT_URL="postgresql://venzory:venzory@localhost:5432/venzory_dev"

# For integration tests (set automatically by vitest, but good to know):
# DATABASE_URL="postgresql://venzory:venzory@localhost:5432/venzory_test"

# Option B: NEON (Staging/Production)
# ------------------------------------------------------------------
# DATABASE_URL="postgres://<user>:<password>@<host>/neondb?sslmode=require&pgbouncer=true"
# DIRECT_URL="postgres://<user>:<password>@<host>/neondb?sslmode=require"


# ============================================
# AUTHENTICATION
# ============================================

# NextAuth Configuration
# Secret for JWT token signing (minimum 32 characters)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here-min-32-chars

# CSRF Protection
# Secret for CSRF token signing (minimum 32 characters)
# Generate with: openssl rand -base64 32
CSRF_SECRET=your-csrf-secret-here-min-32-chars


# ============================================
# DEVELOPMENT DEFAULTS (Auto-set if omitted in development)
# ============================================

# NextAuth URL (auto-defaults to http://localhost:3000 in development)
# MUST use HTTPS in production
NEXTAUTH_URL=http://localhost:3000

# Public App URL (auto-defaults to http://localhost:3000 in development)        
# MUST use HTTPS in production
# Used for email links (password reset, invites)
NEXT_PUBLIC_APP_URL=http://localhost:3000


# ============================================
# REQUIRED IN PRODUCTION ONLY
# ============================================

# Email Service (Resend)
# Required in production for password reset and user invite emails
# Optional in development (logs to console instead)
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here

# Redis Connection
# Required in production for distributed rate limiting
# Optional in development (uses in-memory fallback)
# Format: redis://[[username]:[password]@][host][:port][/db-number]
# Examples:
#   redis://localhost:6379
#   redis://:password@redis.example.com:6379
#   redis://user:pass@redis.example.com:6379/0
REDIS_URL=redis://localhost:6379


# ============================================
# OPTIONAL - ERROR TRACKING (Sentry)
# ============================================

# Server-side error tracking
# Get your DSN from: https://sentry.io/settings/projects/
SENTRY_DSN=

# Client-side error tracking
NEXT_PUBLIC_SENTRY_DSN=

# Sentry organization slug (for source map uploads)
SENTRY_ORG=

# Sentry project slug (for source map uploads)
SENTRY_PROJECT=

# Sentry auth token (for CI/CD source map uploads)
# Generate at: https://sentry.io/settings/account/api/auth-tokens/
SENTRY_AUTH_TOKEN=


# ============================================
# INTERNAL - DO NOT SET MANUALLY
# ============================================

# Next.js runtime detection (automatically set by Next.js)
# NEXT_RUNTIME=nodejs


# ============================================
# QUICK START CHECKLIST
# ============================================
#
# 1. Copy this file: cp .env.example .env.local
#
# 2. Generate secrets:
#    openssl rand -base64 32  # For NEXTAUTH_SECRET
#    openssl rand -base64 32  # For CSRF_SECRET
#
# 3. Set up Local Database:
#    npm run db:setup:local
#
# 4. Optional for development:
#    - Leave RESEND_API_KEY empty (emails log to console)
#    - Leave REDIS_URL empty (uses in-memory rate limiting)
#    - Leave Sentry vars empty (error tracking disabled)
#
# 5. Required for production:
#    - Set RESEND_API_KEY for email functionality
#    - Set REDIS_URL for distributed rate limiting
#    - Update NEXTAUTH_URL and NEXT_PUBLIC_APP_URL to HTTPS URLs
#    - Optionally configure Sentry for error tracking
#
# ============================================

